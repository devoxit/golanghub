#include<windows.h>
#include<stdio.h>

int main(void) {
	HINSTANCE hDll;
	hDll = LoadLibrary(TEXT("vk_shadershim.dll"));
		if (hDll == NULL) {
			printf("dll not found");
		}
		else {
			printf("dll found");
		}
}
